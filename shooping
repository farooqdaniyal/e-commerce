<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="products.css">
    
    <style>
        .add-card button {
            background: #667eea;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s;
            font-weight: 600;
        }
        
        .add-card button:hover {
            background: #5568d3;
            transform: translateY(-2px);
        }
        
        .add-card button:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }
        
        .add-card button.adding {
            background: #27ae60;
        }
    </style>
</head>
<body>
    <?php include "header.php"; ?>

    <div style="height: 100px;"></div>
    
    <div class="products-container">
        <!-- Product 1 -->
        <div class="product-one">
            <div class="product-card">
                <div class="product-two">
                    <img src="img/g-dress-7.avif" alt="">
                </div>
                <div class="new-pro mt-4 margin d-flex" style="margin-left: 10px;">
                    <div class="d-flex newww">
                        <p>new</p>
                        <p>-50%</p>
                    </div>
                    <div class="add-card" style="margin-right: 20px;">
                        <button onclick="addToCart(1, 'Vision Ember Black', 5999, 'img/g-dress-7.avif', this)">
                            <i class="fa-solid fa-cart-shopping"></i> Add to Cart
                        </button>
                    </div>
                </div>    
                <div class="product-info" style="margin-left: 10px;">
                    <p class="black">Vision Ember Unstitched Fabric <br> Blended</p>
                </div>
                <div class="d-flex paragraph" style="margin-left: 10px;">
                    <p>Rs.5,999</p>
                    <p class="pppp">Rs.6,999</p>
                </div>         
            </div>      
        </div>

        <!-- Product 2 -->
        <div class="product-one">
            <div class="product-card">
                <div class="product-two">
                    <img src="img/g-dress-3.avif" alt="">
                </div>
                <div class="new-pro mt-4 margin d-flex" style="margin-left: 10px;">
                    <div class="d-flex newww">
                        <p>new</p>
                        <p>-50%</p>
                    </div>
                    <div class="add-card" style="margin-right: 20px;">
                        <button onclick="addToCart(2, 'Vision Ember Red', 5999, 'img/g-dress-3.avif', this)">
                            <i class="fa-solid fa-cart-shopping"></i> Add to Cart
                        </button>
                    </div>
                </div>    
                <div class="product-info" style="margin-left: 10px;">
                    <p class="black">Vision Ember Unstitched Fabric <br> Blended</p>
                </div>
                <div class="d-flex paragraph" style="margin-left: 10px;">
                    <p>Rs.5,999</p>
                    <p class="pppp">Rs.6,999</p>
                </div>         
            </div>      
        </div>

        <!-- Product 3 -->
        <div class="product-one">
            <div class="product-card">
                <div class="product-two">
                    <img src="img/g-dress-5.avif" alt="">
                </div>
                <div class="new-pro mt-4 margin d-flex" style="margin-left: 10px;">
                    <div class="d-flex newww">
                        <p>new</p>
                        <p>-50%</p>
                    </div>
                    <div class="add-card" style="margin-right: 20px;">
                        <button onclick="addToCart(3, 'Vision Ember Blue', 5999, 'img/g-dress-5.avif', this)">
                            <i class="fa-solid fa-cart-shopping"></i> Add to Cart
                        </button>
                    </div>
                </div>    
                <div class="product-info" style="margin-left: 10px;">
                    <p class="black">Vision Ember Unstitched Fabric <br> Blended</p>
                </div>
                <div class="d-flex paragraph" style="margin-left: 10px;">
                    <p>Rs.5,999</p>
                    <p class="pppp">Rs.6,999</p>
                </div>         
            </div>      
        </div>

        <!-- Continue same pattern for other products... -->
        <!-- Product 4, 5, 6, etc. with onclick="addToCart(ID, NAME, PRICE, IMAGE, this)" -->
    </div>

    <!-- JavaScript -->
    <script>
        function addToCart(productId, productName, productPrice, productImage, button) {
            // Disable button
            button.disabled = true;
            button.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Adding...';
            
            // Send AJAX request
            fetch('php/add_to_cart.php', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/x-www-form-urlencoded',
                },
                body: 'product_id=' + productId + 
                      '&product_name=' + encodeURIComponent(productName) + 
                      '&product_price=' + productPrice + 
                      '&product_image=' + encodeURIComponent(productImage)
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    // Update cart badge
                    const badge = document.getElementById('cartBadge');
                    badge.textContent = data.cart_count;
                    badge.classList.remove('hidden');
                    
                    // Show toast notification
                    showToast('✓ ' + data.message);
                    
                    // Reset button
                    button.classList.add('adding');
                    button.innerHTML = '<i class="fa-solid fa-check"></i> Added!';
                    
                    setTimeout(() => {
                        button.classList.remove('adding');
                        button.innerHTML = '<i class="fa-solid fa-cart-shopping"></i> Add to Cart';
                        button.disabled = false;
                    }, 2000);
                }
            })
            .catch(error => {
                console.error('Error:', error);
                button.innerHTML = '<i class="fa-solid fa-cart-shopping"></i> Add to Cart';
                button.disabled = false;
                showToast('❌ Error adding to cart!', 'error');
            });
        }
        
        function showToast(message, type = 'success') {
            const toast = document.getElementById('toastNotification');
            toast.textContent = message;
            toast.style.background = type === 'success' ? '#27ae60' : '#e74c3c';
            toast.style.display = 'block';
            
            setTimeout(() => {
                toast.style.display = 'none';
            }, 3000);
        }
    </script>

    <!-- Footer -->
    <footer>
        <!-- Your footer code -->
    </footer>
</body>
</html>